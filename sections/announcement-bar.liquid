{% if section.settings.enabled %}
  <div class="mini-announcement">
    <div class="container mini-announcement__inner"><span>{{ section.settings.text }}</span></div>
  </div>
{% endif %}

{% schema %}
{
  "name": "Hinweisleiste",
  "settings": [
    { "type": "checkbox", "id": "enabled", "label": "Anzeigen", "default": true },
    { "type": "text", "id": "text", "label": "Text", "default": "Kostenloser Versand ab 99 € · 30 Tage Rückgaberecht" }
  ]
}
{% endschema %}
